ANTIGRAVITY COMMIT HANDOFF
Branch: main
Rewritten: 2026-09-09 (Entry 023 audit; updated for Entries 024, 026-030)

STATUS: NOTHING IN THIS FILE HAS BEEN COMMITTED OR PUSHED.
This file describes the CURRENT uncommitted working-tree delta only.

The previous version of this file was stale: it named branch
feature/synthetic-dataset-v1 (since merged in 0d0d9d7), listed ~30 files that are
already committed, and claimed data/reports/models_saved were not covered by
.gitignore. They are covered now. All of that has been removed.

--------------------------------------------------------------------------------
PURPOSE (one line)
--------------------------------------------------------------------------------
Analog synthetic signal generation (AM-DSB, AM-SSB, WBFM), the harness safety work
that lets a bit-less capture pass through evaluation without a fabricated BER, and a
minimal analog-aware fusion fallback.

--------------------------------------------------------------------------------
UNCOMMITTED - MODIFIED TRACKED FILES
--------------------------------------------------------------------------------

Production (8):
  src/radiofry/ingestion/iq_parser.py           read a SigMF sidecar's centre
                                                frequency into metadata (Entry 030)
  src/radiofry/ingestion/wav_parser.py          same for WAV (Entry 030)
  src/radiofry/decoding/demodulators/dispatch.py  analog bypasses the digital
                                                symbol-rate timing/decimation path;
                                                ANALOG_LABELS + _demodulate_analog
                                                (Entry 028). Digital untouched.
  src/radiofry/dsp/cyclostationary.py           positive family-level analog test:
                                                frequency_cv < 0.9 AND no fourth-power
                                                line, placed after FSK/PSK and before
                                                QAM; final else now "unknown"
                                                (Entry 027). 0/800 digital FP.
  src/radiofry/fusion/confidence_fusion.py      analog fallback: recover an analog
                                                label from CNN alternatives when a
                                                digital top-1 is rejected and the
                                                classical family is analog-like
                                                (Entry 026). Threshold unchanged.
  src/radiofry/pipeline.py                      pass CNN confidences to fusion as
                                                ranked_alternatives (Entry 026)
  src/radiofry/evaluation/harness.py            analog BER-unavailable guard;
                                                has_source_bits threaded through
                                                evaluate_capture / _score_report
                                                (Entry 021)
  src/radiofry/synthetic_gen/v1/generator.py    load_ground_truth returns None bits
                                                for a null bits block (Entry 021)

Documentation (5):
  BANK.md                                       Entries 021-030 appended
  OP.md                                         Runs 014-022 appended
  ANTIGRAVITY_COMMIT.txt                        this file
  research_memory/CURRENT.md                    rewritten (023); 024; 026-030 status
  research_memory/INDEX.md                      Entry 017, Analog section, 023-030

--------------------------------------------------------------------------------
UNCOMMITTED - UNTRACKED FILES TO ADD (10)
--------------------------------------------------------------------------------

  src/radiofry/ingestion/sidecar.py             minimal SigMF .sigmf-meta reader for
                                                core:frequency / core:sample_rate only;
                                                rejects malformed values (Entry 030)
  src/radiofry/synthetic_gen/v1/analog.py       AM-DSB + AM-SSB + WBFM generation,
                                                separate ANALOG_MODULATIONS registry
                                                (Entries 020, 022, 024)
  tests/test_synthetic_am_dsb.py                31 tests
  tests/test_synthetic_am_ssb.py                28 tests
  tests/test_analog_harness_safety.py           15 tests
  tests/test_synthetic_wbfm.py                  34 tests
  tests/test_fusion_analog_fallback.py          19 tests
  tests/test_classical_analog_detection.py      40 tests
  tests/test_analog_dispatch_bypass.py          32 tests
  tests/test_ingestion_sigmf_metadata.py        32 tests

--------------------------------------------------------------------------------
DO NOT COMMIT
--------------------------------------------------------------------------------

Already excluded by .gitignore - verify they stay out:
  /data/            frozen V1 dataset; fully reproducible from recorded seeds
  /reports/         harness output; regenerable
  /models_saved/    V2.0 checkpoints
  __pycache__/, .pytest_cache/, *.egg-info/

Spent one-off files, left in place for the user to decide on:
  scratch.py        throwaway script that parsed the OLD version of this file to
                    build a commit list. Obsolete now that this file is rewritten.
  commit_msg.txt    a copy of the V1 commit message, which is already committed.

No analog dataset artifacts exist anywhere in the repo - the AM-DSB, AM-SSB and
WBFM tests write captures into pytest tmp_path only.

--------------------------------------------------------------------------------
TEST STATE AT HANDOFF
--------------------------------------------------------------------------------

  python -m pytest -q --ignore=tests/test_model_report.py
  -> 646 passed, 1 failed

The --ignore is required: tests/test_model_report.py imports h5py, which is not
installed, and the collection error aborts the whole session. The one failure is
test_decoding_correlation.py::test_reed_solomon_round_trip - reedsolo is not
installed. Both gaps are environmental and pre-date this work.

Focused regression (9 files incl. fusion, QAM routing, FSK timing): 193 passed.

--------------------------------------------------------------------------------
SUGGESTED COMMIT MESSAGE
--------------------------------------------------------------------------------

feat: add AM-DSB, AM-SSB and WBFM synthetic generation with analog harness safety

Adds analog synthetic capture generation in a registry deliberately separate from
the digital MODULATIONS registry, so the three call sites that default to
tuple(MODULATIONS) do not silently widen. AM-DSB uses a retained carrier
(1 + m*s(t)); AM-SSB uses the analytic signal, with USB as the documented default
and LSB configurable, recorded per capture in ground truth. WBFM integrates the
message phase explicitly - phi[n] = (2*pi/Fs) * cumsum(f_c + df*s[k]) - giving a
constant-envelope waveform whose configured peak deviation is reproduced exactly.

Analog captures carry no bits, no symbol rate and no Es/N0. Two fixes make the
evaluation harness handle that honestly rather than crashing or inventing a metric:
load_ground_truth returns None for a null bits block, and the harness forces
ber_status="unavailable" with reason "analog_no_transmitted_bits" even when
dispatch synthesises threshold bits.

Independent oracles validate generation only - not demodulation. AM-SSB
unwanted-sideband suppression measures 140-176 dB per tone against a 30 dB target.
WBFM peak deviation measures 15000.000 Hz against 15000.0 configured, with noiseless
message-recovery correlation of 1.000000 and monotone degradation under AWGN.

Known limitation, not addressed here: the CNN has no analog class, so analog
captures never reach demodulate_am / demodulate_ssb. AM-DSB is labelled PAM4 and
WBFM is labelled BPSK, which routes WBFM into the 2PSK demodulator; the guard
correctly refuses to score the resulting bits. No classifier, fusion threshold or
dispatch route was changed to hide this. Entry 026 adds a narrow fusion fallback
that recovers an analog label from the CNN's own ranked alternatives when a digital
top-1 is already rejected and the classical family is analog-like; measured benefit
is 1 firing in 15 analog captures, and 0 of 40 frozen V1 captures change.
Entry 027 then gives the classical detector a positive analog test (frequency_cv
below 0.9 with no fourth-power line) in place of decision-by-elimination: 0 of 800
digital controls become analog-like and no digital capture changes family, while
AM-DSB at a non-zero carrier offset now routes to demodulate_am in 4 of 5 seeds.
Entry 028 then makes analog dispatch symbol-rate independent: analog no longer
requires or uses a symbol rate, and no longer decimates by one. WBFM message
recovery through dispatch goes 0.145 -> 1.000, AM-DSB message tones stop aliasing,
and AM-DSB at a non-zero carrier offset recovers 0.94 correlation end to end.
Digital dispatch, including both timing-offset heuristics, is unchanged.
Entry 030 closes the SSB carrier gap at the ingestion layer: a SigMF .sigmf-meta
sidecar now populates metadata["center_frequency_hz"], which estimate_parameters
already preferred and preprocess already preserved. SSB recovers at 0.995-1.000
with a sidecar and is unchanged without one. The synthetic generator writes no
sidecars, so ground truth can never reach production ingestion.

Tests: 646 passed (test_model_report.py ignored - h5py absent). The single failure,
test_reed_solomon_round_trip, is pre-existing and environmental.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
